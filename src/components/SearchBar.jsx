import React from 'react';

const SearchBar = () => (
  <section>
    <input
      type="text"
      data-testid="search-input"
      placeholder="Buscar Receita!"
    />
  </section>
);

export default SearchBar;
